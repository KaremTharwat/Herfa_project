import 'package:flutter/material.dart';

class GeneralSignup extends StatelessWidget {
  const GeneralSignup({super.key});
  static final generalSignup = "generalSignup";
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [],
      ),
    );
  }
}